<% root_url = "http://4pdj.nl/" %>
<ul>
    <% sitemap.resources.select { |page| page.path =~ /\.html/ }.each do |page| %>
         <li><a href="<%= "#{root_url}#{page.path}" %>"><%= "#{root_url}#{page.path}" %></a></li>
    <% end %>
</ul>
